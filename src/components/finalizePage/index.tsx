import BackLink from '../backLink';
import styles from './finalize.module.css';

const FinalizePage = () => {
  return (
    <div className={styles['finalize-page']}>
      <h1>Finalize</h1>
      <h2>Spring promotion</h2>
      <BackLink />
    </div>
  );
};

export default FinalizePage;
